import numpy as np
from Chi import lineChi

class Minimiser(object):
    def __init__(self,function,params):
        self.param=params
        self.function=function
    def minimise(self,paramg,function):
        if (self.function(self.params) )
        function(paramg)
